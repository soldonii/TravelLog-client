import React from 'react';
import { mount } from 'enzyme';
import MyTravel from '../MyTravel';

describe('<MyTravel /> component', () => {
  it('should render several travel props.', () => {
    

    const wrapper = mount(<MyTravel />);
  });
};

userId,
  allTravels,
  getAllTravelData,
  changeTravelId